# 1️⃣ Supprime le suivi Git dans client mais garde les fichiers
git rm -r --cached client

# 2️⃣ Réajoute le dossier client comme dossier normal
git add client

# 3️⃣ Commit et push pour GitHub
git commit -m "Fix: reset client folder to normal folder"
git push





git add .

git commit -m "Add files"

git push