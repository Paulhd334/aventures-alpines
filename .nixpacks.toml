[phases.setup]
nixPkgs = ['nodejs_20']  # Utilise Node 20 comme dans ton package.json

[phases.install]
cmds = ['cd server && npm ci --only=production']

[start]
cmd = 'cd server && npm start'  # Utilise cmd au singulier, pas cmds