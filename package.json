{
  "name": "aventures-alpines",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && SKIP_PREFLIGHT_CHECK=true GENERATE_SOURCEMAP=false CI=false npx react-scripts build"
  },
  "dependencies": {
    "http-proxy-middleware": "^3.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
