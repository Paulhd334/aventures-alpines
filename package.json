{
  "name": "aventures-alpines",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
   "build": "SKIP_PREFLIGHT_CHECK=true cd client && npm install && GENERATE_SOURCEMAP=false react-scripts build"

  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
