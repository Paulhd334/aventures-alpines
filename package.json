{
  "name": "aventures-alpines",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "CI=false SKIP_PREFLIGHT_CHECK=true cd client && npm install && npm run build"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
